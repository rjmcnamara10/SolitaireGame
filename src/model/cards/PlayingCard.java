package model.cards;

/**
 * Represents an interface containing the base methods necessary for a playing card in a game of
 * solitaire.
 */
public interface PlayingCard {

  /**
   * Changes the visibility of the card to the opposite of what it is currently.
   */
  void flipCard();
}
